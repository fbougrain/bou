/// Central enum describing top-level sections in the app.
/// First four correspond to bottom navigation; the rest are secondary tabs.
enum MainSection {
  home,
  project,
  media,
  profile,
  tasks,
  stocks,
  forms,
  billing,
}
